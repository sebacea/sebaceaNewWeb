---
title: "Blog"
subtitle: A data science & R-Stats blog by Karat Sidhu
listing:
  type: table
  fields: [image, title, description, date]
  page-size: 10
  feed: true
  contents: posts
  sort-ui: false
  filter-ui: false
  sort: "date desc"
  categories: true
page-layout: full
title-block-banner: true
image: images/karat.png
---

```{r write-redirects}
#| echo: false
# list names of post folders
posts <- list.dirs(
  path = here::here("posts"),
  full.names = FALSE,
  recursive = FALSE
)

# extract the slugs
slugs <- gsub("^.*_", "", posts)

# lines to insert to a netlify _redirect file
redirects <- paste0("/", slugs, " ", "/posts/", posts)

# write the _redirect file
writeLines(redirects, here::here("_site", "_redirects"))
```
